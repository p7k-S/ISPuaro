Строки, связанные с GCC:

GCC_EXEC_PREFIX

gcc version %s

../../src/gcc/gcc.c

Using built-in specs.

COLLECT_GCC_OPTIONS

COLLECT_GCC=gcc-4.1

4.1.2 20061115 (prerelease) (Debian 4.1.1-21)

Строки, указывающие на версию и сборку:

4.1.2 — версия GCC.

Debian 4.1.1-21 — указывает на дистрибутив и пакетную сборку.

x86_64-linux-gnu — целевая архитектура.

Строки, связанные с компонентами GCC:

cc1 (основной компилятор C),

cc1plus (компилятор C++),

collect2 (утилита для линковки),

libgcc, libstdc++ (стандартные библиотеки).

Строки из спецификаций (specs):

Много строк, начинающихся с %{...}, которые являются частью файла спецификаций GCC (например, %{!shared:%{!static:%{!static-libgcc:...}}}).

Строки из runtime-библиотек:

GLIBC_2.3, GLIBC_2.2.5 — версии glibc, с которыми связан GCC.

Пути и системные вызовы:

/usr/lib/gcc/, /usr/bin/, /usr/libexec/gcc/ — стандартные пути установки GCC.

Системные вызовы, такие как execv, vfork, pipe, waitpid, которые используются драйвером GCC.
